[package]
name = "Starling-TUI"
version = "0.5.29"
edition = "2024"

[[bin]]
name = "starling-tui"
path = "src/main.rs"

[features]
default = ["audio", "video"]
audio = ["dep:cpal", "dep:ringbuf"]
video = ["dep:nokhwa"]

[dependencies]
anyhow = "1.0.104"
arboard = "=3.6.1"
chrono = { version = "0.4.45", features = ["serde"] }
cpal = { version = "0.18.1", optional = true }
crossterm = { version = "0.29.0", features = ["event-stream"] }
image = "0.25.10"
iroh = "1.0.3"
iroh-gossip = "0.101.0"
n0-future = "0.3.2"
nokhwa = { version = "0.10.11", features = ["input-native"], optional = true }
postcard = { version = "1.1.3", features = ["use-std"] }
ratatui = "0.30.2"
ringbuf = { version = "0.5.1", optional = true }
serde = { version = "1.0.229", features = ["derive"] }
sha2 = "0.11.0"
sled = "0.34"
starling = { git = "https://forgejo.hearthhome.lol/Saltfault/Starling.git" }
tokio = { version = "1.53.1", features = ["full"] }
tokio-util = { version = "0.7.18", features = ["rt"] }
uuid = { version = "1.24.0", features = ["v4", "serde"] }
zeroize = "1.8.2"

[dev-dependencies]
tempfile = "3.23.0"

[patch."https://forgejo.hearthhome.lol/Saltfault/Starling.git"]
starling = { path = "../starling" }

# Linux: cpal PulseAudio backend, nokhwa (V4L2 — needs libclang-dev)
[target.'cfg(target_os = "linux")'.dependencies]
cpal = { version = "0.18.1", features = ["pulseaudio"], optional = true }
nokhwa = { version = "0.10.11", features = ["input-native"], optional = true }

# Windows: nokhwa (Media Foundation — no extra deps needed)
[target.'cfg(target_os = "windows")'.dependencies]
nokhwa = { version = "0.10.11", features = ["input-native"], optional = true }

# macOS: nokhwa (AVFoundation — no extra deps needed)
[target.'cfg(target_os = "macos")'.dependencies]
nokhwa = { version = "0.10.11", features = ["input-native"], optional = true }
